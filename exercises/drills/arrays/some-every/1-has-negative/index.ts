/**
 * Check if any number in the array is negative.
 *
 * Use: arr.some()
 *
 * Examples:
 *   hasNegative([1, -2, 3])  → true
 *   hasNegative([1, 2, 3])   → false
 *   hasNegative([-1])        → true
 *   hasNegative([0, 0, 0])   → false
 */
export function hasNegative(nums: number[]): boolean {
  throw new Error("Not implemented");
}
