/**
 * Check if any string in the array is empty.
 *
 * Use: arr.some()
 *
 * Examples:
 *   hasEmptyString(["hi", "", "bye"])  → true
 *   hasEmptyString(["hi", "bye"])      → false
 *   hasEmptyString([""])               → true
 *   hasEmptyString(["a", "b", "c"])    → false
 */
export function hasEmptyString(strs: string[]): boolean {
  throw new Error("Not implemented");
}
