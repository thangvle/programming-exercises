/**
 * Check if no element in the array matches the given value.
 *
 * Use: arr.some() or arr.every()
 *
 * Examples:
 *   noneMatch([1, 2, 3], 4)      → true
 *   noneMatch([1, 2, 3], 2)      → false
 *   noneMatch(["a", "b"], "c")   → true
 *   noneMatch(["a", "b"], "a")   → false
 */
export function noneMatch<T>(arr: T[], value: T): boolean {
  throw new Error("Not implemented");
}
