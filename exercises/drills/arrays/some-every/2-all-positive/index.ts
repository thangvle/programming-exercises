/**
 * Check if all numbers in the array are positive (greater than 0).
 *
 * Use: arr.every()
 *
 * Examples:
 *   allPositive([1, 2, 3])   → true
 *   allPositive([1, -2, 3])  → false
 *   allPositive([0, 1, 2])   → false
 *   allPositive([5])         → true
 */
export function allPositive(nums: number[]): boolean {
  throw new Error("Not implemented");
}
