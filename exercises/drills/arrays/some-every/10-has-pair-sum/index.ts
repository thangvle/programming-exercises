/**
 * Check if any two numbers in the array sum to the target.
 *
 * Use: arr.some()
 *
 * Examples:
 *   hasPairSum([1, 2, 3, 4], 5)  → true
 *   hasPairSum([1, 2, 3], 10)    → false
 *   hasPairSum([5, 5], 10)       → true
 *   hasPairSum([1], 2)           → false
 */
export function hasPairSum(nums: number[], target: number): boolean {
  throw new Error("Not implemented");
}
