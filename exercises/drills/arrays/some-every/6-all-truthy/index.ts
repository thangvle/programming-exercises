/**
 * Check if all values in the array are truthy.
 *
 * Use: arr.every()
 *
 * Examples:
 *   allTruthy([1, "hi", true])      → true
 *   allTruthy([1, 0, true])         → false
 *   allTruthy([null, "hi"])         → false
 *   allTruthy(["a", 1, {}, []])     → true
 */
export function allTruthy(values: unknown[]): boolean {
  throw new Error("Not implemented");
}
