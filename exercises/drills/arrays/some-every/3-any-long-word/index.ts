/**
 * Check if any word in the array is longer than n characters.
 *
 * Use: arr.some()
 *
 * Examples:
 *   anyLongWord(["hi", "hello"], 4)        → true
 *   anyLongWord(["hi", "hey"], 4)          → false
 *   anyLongWord(["short", "tiny"], 5)      → false
 *   anyLongWord(["extraordinary"], 5)      → true
 */
export function anyLongWord(words: string[], n: number): boolean {
  throw new Error("Not implemented");
}
