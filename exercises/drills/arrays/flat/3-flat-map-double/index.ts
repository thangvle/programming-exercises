/**
 * Double each element so it appears twice in the result.
 *
 * Use: arr.flatMap()
 *
 * Examples:
 *   flatMapDouble([1, 2, 3])     → [1, 1, 2, 2, 3, 3]
 *   flatMapDouble([5])           → [5, 5]
 *   flatMapDouble(["a", "b"])    → ["a", "a", "b", "b"]
 *   flatMapDouble([0])           → [0, 0]
 */
export function flatMapDouble<T>(arr: T[]): T[] {
  throw new Error("Not implemented");
}
