/**
 * Split each string by space and flatten into a single array.
 *
 * Use: arr.flatMap(), str.split()
 *
 * Examples:
 *   splitAndFlatten(["hello world", "foo bar"])  → ["hello", "world", "foo", "bar"]
 *   splitAndFlatten(["one two three"])            → ["one", "two", "three"]
 *   splitAndFlatten(["single"])                   → ["single"]
 *   splitAndFlatten(["a b", "c d", "e f"])        → ["a", "b", "c", "d", "e", "f"]
 */
export function splitAndFlatten(strs: string[]): string[] {
  throw new Error("Not implemented");
}
