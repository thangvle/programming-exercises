/**
 * Extract all words from an array of sentences.
 *
 * Use: arr.flatMap(), str.split()
 *
 * Examples:
 *   wordsFromSentences(["hello world", "good morning"])  → ["hello", "world", "good", "morning"]
 *   wordsFromSentences(["one two three"])                → ["one", "two", "three"]
 *   wordsFromSentences(["hi"])                           → ["hi"]
 *   wordsFromSentences(["a b", "c d e"])                 → ["a", "b", "c", "d", "e"]
 */
export function wordsFromSentences(sentences: string[]): string[] {
  throw new Error("Not implemented");
}
