/**
 * Merge an array of arrays into a single array.
 *
 * Use: arr.flat()
 *
 * Examples:
 *   mergeArrays([[1], [2, 3], [4, 5, 6]])  → [1, 2, 3, 4, 5, 6]
 *   mergeArrays([["a"], ["b", "c"]])        → ["a", "b", "c"]
 *   mergeArrays([[1, 2]])                   → [1, 2]
 *   mergeArrays([[], [1], []])              → [1]
 */
export function mergeArrays<T>(arrays: T[][]): T[] {
  throw new Error("Not implemented");
}
