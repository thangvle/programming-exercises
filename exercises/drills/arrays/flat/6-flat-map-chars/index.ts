/**
 * Split each string into individual characters and flatten.
 *
 * Use: arr.flatMap(), str.split()
 *
 * Examples:
 *   flatMapChars(["hi", "ok"])     → ["h", "i", "o", "k"]
 *   flatMapChars(["abc"])          → ["a", "b", "c"]
 *   flatMapChars(["a", "b"])       → ["a", "b"]
 *   flatMapChars(["hey", "yo"])    → ["h", "e", "y", "y", "o"]
 */
export function flatMapChars(strs: string[]): string[] {
  throw new Error("Not implemented");
}
