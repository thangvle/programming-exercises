/**
 * Find the index of the first element matching a value.
 *
 * Use: Array.prototype.findIndex
 *
 * @example
 * findIndexOf([10, 20, 30], 20) → 1
 * findIndexOf([1, 2, 3], 5) → -1
 */
export function findIndexOf<T>(arr: T[], value: T): number {
  throw new Error("Not implemented");
}
