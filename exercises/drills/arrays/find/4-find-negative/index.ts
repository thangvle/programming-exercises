/**
 * Find the first negative number in the array.
 *
 * Use: Array.prototype.find
 *
 * @example
 * findNegative([1, 2, -3, 4]) → -3
 * findNegative([1, 2, 3]) → undefined
 */
export function findNegative(nums: number[]): number | undefined {
  throw new Error("Not implemented");
}
