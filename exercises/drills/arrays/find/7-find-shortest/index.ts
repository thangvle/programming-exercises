/**
 * Find the shortest string in the array.
 *
 * Use: Array.prototype.reduce
 *
 * @example
 * findShortest(["hello", "hi", "hey"]) → "hi"
 * findShortest(["a", "bb", "ccc"]) → "a"
 */
export function findShortest(strings: string[]): string {
  throw new Error("Not implemented");
}
