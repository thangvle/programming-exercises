/**
 * Find the first pair of numbers that sum to the target. Return them as a tuple.
 *
 * Use: Array.prototype.find or nested iteration
 *
 * @example
 * findPairSum([1, 2, 3, 4], 5) → [1, 4] or [2, 3]
 * findPairSum([1, 2], 10) → undefined
 */
export function findPairSum(
  nums: number[],
  target: number
): [number, number] | undefined {
  throw new Error("Not implemented");
}
