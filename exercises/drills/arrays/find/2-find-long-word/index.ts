/**
 * Find the first word longer than n characters.
 *
 * Use: Array.prototype.find
 *
 * @example
 * findLongWord(["hi", "hello", "hey"], 3) → "hello"
 * findLongWord(["a", "b"], 5) → undefined
 */
export function findLongWord(
  words: string[],
  n: number
): string | undefined {
  throw new Error("Not implemented");
}
