/**
 * Find the first missing number in a sequence starting from the smallest value.
 *
 * Use: Array.prototype.find or a loop
 *
 * @example
 * findMissing([1, 2, 4, 5]) → 3
 * findMissing([1, 2, 3]) → undefined
 */
export function findMissing(nums: number[]): number | undefined {
  throw new Error("Not implemented");
}
