/**
 * Wrap each string in double quotes and join with ", ".
 *
 * Use: arr.map(), arr.join()
 *
 * Examples:
 *   wrapAndJoin(["a", "b", "c"])  → '"a", "b", "c"'
 *   wrapAndJoin(["hello"])        → '"hello"'
 *   wrapAndJoin(["x", "y"])       → '"x", "y"'
 *   wrapAndJoin(["one", "two"])   → '"one", "two"'
 */
export function wrapAndJoin(strs: string[]): string {
  throw new Error("Not implemented");
}
