/**
 * Convert each digit of a number to its English word and join with spaces.
 *
 * Use: String(), str.split(), arr.map(), arr.join()
 *
 * Examples:
 *   digitWords(123)   → "one two three"
 *   digitWords(405)   → "four zero five"
 *   digitWords(7)     → "seven"
 *   digitWords(90)    → "nine zero"
 */
export function digitWords(num: number): string {
  throw new Error("Not implemented");
}
