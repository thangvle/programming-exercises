/**
 * Wrap each item in <li> tags and join them together.
 *
 * Use: arr.map(), arr.join()
 *
 * Examples:
 *   toHtmlList(["a", "b"])         → "<li>a</li><li>b</li>"
 *   toHtmlList(["hello"])          → "<li>hello</li>"
 *   toHtmlList(["x", "y", "z"])   → "<li>x</li><li>y</li><li>z</li>"
 *   toHtmlList(["one", "two"])     → "<li>one</li><li>two</li>"
 */
export function toHtmlList(items: string[]): string {
  throw new Error("Not implemented");
}
