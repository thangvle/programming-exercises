/**
 * Join values with commas for a CSV line.
 *
 * Use: arr.join()
 *
 * Examples:
 *   toCsvLine(["name", "age", "city"])  → "name,age,city"
 *   toCsvLine(["a", "b"])              → "a,b"
 *   toCsvLine(["only"])                → "only"
 *   toCsvLine(["1", "2", "3", "4"])    → "1,2,3,4"
 */
export function toCsvLine(values: string[]): string {
  throw new Error("Not implemented");
}
