/**
 * Join an array of strings with spaces.
 *
 * Use: arr.join()
 *
 * Examples:
 *   joinWithSpace(["hello", "world"])    → "hello world"
 *   joinWithSpace(["one", "two", "three"]) → "one two three"
 *   joinWithSpace(["hi"])                → "hi"
 *   joinWithSpace(["a", "b"])            → "a b"
 */
export function joinWithSpace(strs: string[]): string {
  throw new Error("Not implemented");
}
