/**
 * Join an array of strings with commas.
 *
 * Use: arr.join()
 *
 * Examples:
 *   joinWithComma(["a", "b", "c"])  → "a,b,c"
 *   joinWithComma(["hello"])        → "hello"
 *   joinWithComma(["x", "y"])       → "x,y"
 *   joinWithComma(["1", "2", "3"])  → "1,2,3"
 */
export function joinWithComma(strs: string[]): string {
  throw new Error("Not implemented");
}
