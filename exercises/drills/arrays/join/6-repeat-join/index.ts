/**
 * Create a string by repeating a value n times joined by a separator.
 *
 * Use: Array(n).fill(), arr.join()
 *
 * Examples:
 *   repeatJoin("ha", 3, " ")   → "ha ha ha"
 *   repeatJoin("na", 4, "-")   → "na-na-na-na"
 *   repeatJoin("ok", 1, ",")   → "ok"
 *   repeatJoin("yo", 2, " ")   → "yo yo"
 */
export function repeatJoin(value: string, n: number, separator: string): string {
  throw new Error("Not implemented");
}
