/**
 * Return elements that are present in both arrays.
 *
 * Use: Array.prototype.filter and Array.prototype.includes
 *
 * @example
 * commonElements([1, 2, 3], [2, 3, 4]) → [2, 3]
 * commonElements([1, 2], [3, 4]) → []
 */
export function commonElements<T>(a: T[], b: T[]): T[] {
  throw new Error("Not implemented");
}
