/**
 * Find the last position (index) of a value in the array.
 *
 * Use: Array.prototype.lastIndexOf
 *
 * @example
 * lastPositionOf([1, 2, 3, 2, 1], 2) → 3
 * lastPositionOf([1, 2, 3], 5) → -1
 */
export function lastPositionOf<T>(arr: T[], value: T): number {
  throw new Error("Not implemented");
}
