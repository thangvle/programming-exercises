/**
 * Check if any value from the second array exists in the first array.
 *
 * Use: Array.prototype.some and Array.prototype.includes
 *
 * @example
 * anyPresent([1, 2, 3], [4, 5, 2]) → true
 * anyPresent([1, 2, 3], [4, 5, 6]) → false
 */
export function anyPresent<T>(arr: T[], values: T[]): boolean {
  throw new Error("Not implemented");
}
