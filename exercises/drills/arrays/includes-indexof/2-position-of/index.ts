/**
 * Find the position (index) of a value in the array.
 *
 * Use: Array.prototype.indexOf
 *
 * @example
 * positionOf(["a", "b", "c"], "b") → 1
 * positionOf([10, 20, 30], 30) → 2
 */
export function positionOf<T>(arr: T[], value: T): number {
  throw new Error("Not implemented");
}
