/**
 * Return elements that are in the first array but not in the second.
 *
 * Use: Array.prototype.filter and Array.prototype.includes
 *
 * @example
 * missingElements([1, 2, 3, 4], [2, 4]) → [1, 3]
 * missingElements([1, 2], [1, 2]) → []
 */
export function missingElements<T>(a: T[], b: T[]): T[] {
  throw new Error("Not implemented");
}
