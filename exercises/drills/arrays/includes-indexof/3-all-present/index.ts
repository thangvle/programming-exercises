/**
 * Check if all values from the second array exist in the first array.
 *
 * Use: Array.prototype.every and Array.prototype.includes
 *
 * @example
 * allPresent([1, 2, 3, 4], [2, 4]) → true
 * allPresent([1, 2, 3], [2, 5]) → false
 */
export function allPresent<T>(arr: T[], values: T[]): boolean {
  throw new Error("Not implemented");
}
