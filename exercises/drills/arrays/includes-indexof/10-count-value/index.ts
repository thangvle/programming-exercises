/**
 * Count how many times a value appears in the array.
 *
 * Use: Array.prototype.filter or Array.prototype.reduce
 *
 * @example
 * countValue([1, 2, 2, 3, 2], 2) → 3
 * countValue([1, 2, 3], 5) → 0
 */
export function countValue<T>(arr: T[], value: T): number {
  throw new Error("Not implemented");
}
