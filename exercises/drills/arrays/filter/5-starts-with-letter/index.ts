/**
 * Keep strings that start with a given letter (case-insensitive).
 *
 * Use: Array.prototype.filter
 *
 * @example
 * startsWithLetter(["apple", "banana", "avocado"], "a") → ["apple", "avocado"]
 * startsWithLetter(["Cat", "car", "dog"], "c") → ["Cat", "car"]
 */
export function startsWithLetter(strings: string[], letter: string): string[] {
  throw new Error("Not implemented");
}
