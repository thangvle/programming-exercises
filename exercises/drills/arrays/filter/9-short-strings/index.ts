/**
 * Keep strings with length less than or equal to n.
 *
 * Use: Array.prototype.filter
 *
 * @example
 * shortStrings(["a", "bb", "ccc", "dddd"], 2) → ["a", "bb"]
 * shortStrings(["hello", "hi"], 3) → ["hi"]
 */
export function shortStrings(strings: string[], n: number): string[] {
  throw new Error("Not implemented");
}
