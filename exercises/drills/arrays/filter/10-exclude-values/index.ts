/**
 * Remove specific values from the array.
 *
 * Use: Array.prototype.filter
 *
 * @example
 * excludeValues([1, 2, 3, 4, 5], [2, 4]) → [1, 3, 5]
 * excludeValues(["a", "b", "c"], ["b"]) → ["a", "c"]
 */
export function excludeValues<T>(values: T[], exclude: T[]): T[] {
  throw new Error("Not implemented");
}
