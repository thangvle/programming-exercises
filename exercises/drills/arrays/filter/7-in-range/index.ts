/**
 * Keep numbers within a min-max range (inclusive).
 *
 * Use: Array.prototype.filter
 *
 * @example
 * inRange([1, 5, 10, 15, 20], 5, 15) → [5, 10, 15]
 * inRange([1, 2, 3], 1, 2) → [1, 2]
 */
export function inRange(nums: number[], min: number, max: number): number[] {
  throw new Error("Not implemented");
}
