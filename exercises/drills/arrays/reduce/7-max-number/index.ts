/**
 * Find the maximum number in the array without using Math.max.
 *
 * Use: Array.prototype.reduce
 *
 * @example
 * maxNumber([3, 1, 4, 1, 5]) → 5
 * maxNumber([-1, -5, -2]) → -1
 */
export function maxNumber(nums: number[]): number {
  throw new Error("Not implemented");
}
