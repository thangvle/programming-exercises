/**
 * Sort an array of strings by length, shortest first.
 *
 * Use: arr.sort()
 *
 * Examples:
 *   sortByLength(["hello", "hi", "hey"])  → ["hi", "hey", "hello"]
 *   sortByLength(["a", "bbb", "cc"])      → ["a", "cc", "bbb"]
 *   sortByLength(["same", "size"])        → ["same", "size"]
 *   sortByLength(["one"])                 → ["one"]
 */
export function sortByLength(strs: string[]): string[] {
  throw new Error("Not implemented");
}
