/**
 * Sort an array of strings alphabetically.
 *
 * Use: arr.sort()
 *
 * Examples:
 *   sortAlphabetical(["banana", "apple", "cherry"])  → ["apple", "banana", "cherry"]
 *   sortAlphabetical(["z", "a", "m"])                → ["a", "m", "z"]
 *   sortAlphabetical(["one"])                        → ["one"]
 *   sortAlphabetical(["dog", "cat", "bird"])         → ["bird", "cat", "dog"]
 */
export function sortAlphabetical(strs: string[]): string[] {
  throw new Error("Not implemented");
}
