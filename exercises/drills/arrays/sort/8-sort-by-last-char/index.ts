/**
 * Sort strings by their last character (ascending).
 *
 * Use: arr.sort()
 *
 * Examples:
 *   sortByLastChar(["cab", "abc", "bca"])  → ["bca", "cab", "abc"]
 *   sortByLastChar(["az", "by", "cx"])     → ["cx", "by", "az"]
 *   sortByLastChar(["hello"])              → ["hello"]
 *   sortByLastChar(["bed", "ace", "bag"])  → ["ace", "bed", "bag"]
 */
export function sortByLastChar(strs: string[]): string[] {
  throw new Error("Not implemented");
}
