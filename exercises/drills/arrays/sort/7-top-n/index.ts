/**
 * Return the top n largest numbers, sorted descending.
 *
 * Use: arr.sort(), arr.slice()
 *
 * Examples:
 *   topN([5, 1, 3, 4, 2], 3)  → [5, 4, 3]
 *   topN([10, 20, 30], 2)     → [30, 20]
 *   topN([1, 2, 3], 5)        → [3, 2, 1]
 *   topN([7], 1)              → [7]
 */
export function topN(nums: number[], n: number): number[] {
  throw new Error("Not implemented");
}
