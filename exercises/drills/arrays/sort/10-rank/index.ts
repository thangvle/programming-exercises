/**
 * Return an array showing each element's rank (1-based, highest = 1).
 *
 * Use: arr.sort(), arr.map()
 *
 * Examples:
 *   rank([40, 10, 30, 20])  → [1, 4, 2, 3]
 *   rank([100, 200])        → [2, 1]
 *   rank([5])               → [1]
 *   rank([3, 3, 1])         → [1, 1, 3]
 */
export function rank(nums: number[]): number[] {
  throw new Error("Not implemented");
}
