/**
 * Convert each string in the array to uppercase.
 *
 * Use: Array.prototype.map
 *
 * @example
 * toUpperCase(["hi", "bye"]) → ["HI", "BYE"]
 * toUpperCase(["hello"]) → ["HELLO"]
 */
export function toUpperCase(strings: string[]): string[] {
  throw new Error("Not implemented");
}
