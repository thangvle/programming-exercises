/**
 * Extract the "name" property from each object in the array.
 *
 * Use: Array.prototype.map
 *
 * @example
 * extractNames([{ name: "Alice" }, { name: "Bob" }]) → ["Alice", "Bob"]
 * extractNames([{ name: "Charlie" }]) → ["Charlie"]
 */
export function extractNames(
  objects: { name: string }[]
): string[] {
  throw new Error("Not implemented");
}
