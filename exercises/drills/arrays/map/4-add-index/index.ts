/**
 * Add each element's index to its value.
 *
 * Use: Array.prototype.map
 *
 * @example
 * addIndex([10, 20, 30]) → [10, 21, 32]
 * addIndex([0, 0, 0]) → [0, 1, 2]
 */
export function addIndex(nums: number[]): number[] {
  throw new Error("Not implemented");
}
