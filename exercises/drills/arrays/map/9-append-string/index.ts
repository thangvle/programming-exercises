/**
 * Append a suffix to each string in the array.
 *
 * Use: Array.prototype.map
 *
 * @example
 * appendString(["file1", "file2"], ".txt") → ["file1.txt", "file2.txt"]
 * appendString(["a", "b"], "!") → ["a!", "b!"]
 */
export function appendString(strings: string[], suffix: string): string[] {
  throw new Error("Not implemented");
}
