/**
 * Parse an array of numeric strings into numbers.
 *
 * Use: Array.prototype.map
 *
 * @example
 * parseIntegers(["1", "2", "3"]) → [1, 2, 3]
 * parseIntegers(["10", "20"]) → [10, 20]
 */
export function parseIntegers(strings: string[]): number[] {
  throw new Error("Not implemented");
}
