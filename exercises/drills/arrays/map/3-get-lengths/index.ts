/**
 * Get the length of each string in the array.
 *
 * Use: Array.prototype.map
 *
 * @example
 * getLengths(["hi", "hello"]) → [2, 5]
 * getLengths(["a", "bb", "ccc"]) → [1, 2, 3]
 */
export function getLengths(strings: string[]): number[] {
  throw new Error("Not implemented");
}
