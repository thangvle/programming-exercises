/**
 * Return the array without the first element.
 *
 * Use: arr.slice()
 *
 * Examples:
 *   withoutFirst([1, 2, 3])     → [2, 3]
 *   withoutFirst(["a", "b"])    → ["b"]
 *   withoutFirst([1])           → []
 *   withoutFirst([5, 10, 15])   → [10, 15]
 */
export function withoutFirst<T>(arr: T[]): T[] {
  throw new Error("Not implemented");
}
