/**
 * Return the array without the last element.
 *
 * Use: arr.slice()
 *
 * Examples:
 *   withoutLast([1, 2, 3])     → [1, 2]
 *   withoutLast(["a", "b"])    → ["a"]
 *   withoutLast([1])           → []
 *   withoutLast([5, 10, 15])   → [5, 10]
 */
export function withoutLast<T>(arr: T[]): T[] {
  throw new Error("Not implemented");
}
