/**
 * Insert a value at a given index (return a new array).
 *
 * Use: arr.slice(), spread operator
 *
 * Examples:
 *   insertAt([1, 2, 4, 5], 2, 3)     → [1, 2, 3, 4, 5]
 *   insertAt(["a", "c"], 1, "b")     → ["a", "b", "c"]
 *   insertAt([1, 2], 0, 0)           → [0, 1, 2]
 *   insertAt([1, 2], 2, 3)           → [1, 2, 3]
 */
export function insertAt<T>(arr: T[], index: number, value: T): T[] {
  throw new Error("Not implemented");
}
