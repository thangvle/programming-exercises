/**
 * Count occurrences of each item in an array.
 *
 * Use: .reduce, Object.fromEntries or direct accumulation
 *
 * @example countItems(["a", "b", "a", "c", "b", "a"]) → {a: 3, b: 2, c: 1}
 * @example countItems([]) → {}
 */
export function countItems(items: string[]): Record<string, number> {
  throw new Error("Not implemented");
}
