/**
 * Combine a keys array and a values array into an object.
 *
 * Use: .map, Object.fromEntries
 *
 * @example zipToObject(["a", "b"], [1, 2]) → {a: 1, b: 2}
 * @example zipToObject([], []) → {}
 */
export function zipToObject(keys: string[], values: unknown[]): Record<string, unknown> {
  throw new Error("Not implemented");
}
