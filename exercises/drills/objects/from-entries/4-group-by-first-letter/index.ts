/**
 * Group words by their first letter (lowercase).
 *
 * Use: .reduce, first character access
 *
 * @example groupByFirstLetter(["apple", "banana", "avocado"]) → {a: ["apple", "avocado"], b: ["banana"]}
 * @example groupByFirstLetter([]) → {}
 */
export function groupByFirstLetter(words: string[]): Record<string, string[]> {
  throw new Error("Not implemented");
}
