/**
 * Convert an array of [key, value] pairs to an object.
 *
 * Use: Object.fromEntries
 *
 * @example pairsToObject([["a", 1], ["b", 2]]) → {a: 1, b: 2}
 * @example pairsToObject([]) → {}
 */
export function pairsToObject(pairs: [string, unknown][]): Record<string, unknown> {
  throw new Error("Not implemented");
}
