/**
 * Find the difference between the max and min values in an array.
 *
 * Use: Math.max, Math.min, spread
 *
 * @example rangeSize([3, 1, 4, 1, 5]) → 4
 * @example rangeSize([5, 5, 5]) → 0
 */
export function rangeSize(nums: number[]): number {
  throw new Error("Not implemented");
}
