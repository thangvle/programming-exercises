/**
 * Find the second largest value in an array.
 *
 * Use: .sort, Set for deduplication, or iterative approach
 *
 * @example secondLargest([3, 1, 4, 1, 5]) → 4
 * @example secondLargest([10, 5]) → 5
 */
export function secondLargest(nums: number[]): number {
  throw new Error("Not implemented");
}
