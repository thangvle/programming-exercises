/**
 * Find the minimum value in an array.
 *
 * Use: Math.min with spread, or .reduce
 *
 * @example findMin([3, 1, 4, 1, 5]) → 1
 * @example findMin([10]) → 10
 */
export function findMin(nums: number[]): number {
  throw new Error("Not implemented");
}
