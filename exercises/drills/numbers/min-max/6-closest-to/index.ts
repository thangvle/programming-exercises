/**
 * Find the number in an array closest to a target value.
 *
 * Use: .reduce, Math.abs
 *
 * @example closestTo([1, 5, 10, 15], 8) → 10
 * @example closestTo([1, 5, 10], 3) → 5
 */
export function closestTo(nums: number[], target: number): number {
  throw new Error("Not implemented");
}
