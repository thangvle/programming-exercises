/**
 * Return an object with the min and max of an array.
 *
 * Use: Math.min, Math.max, spread
 *
 * @example minMax([3, 1, 4]) → {min: 1, max: 4}
 * @example minMax([5]) → {min: 5, max: 5}
 */
export function minMax(nums: number[]): { min: number; max: number } {
  throw new Error("Not implemented");
}
