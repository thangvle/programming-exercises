/**
 * Format a GPA to 2 decimal places.
 *
 * Use: .toFixed
 *
 * @example formatGpa(3.8) → "3.80"
 * @example formatGpa(4) → "4.00"
 */
export function formatGpa(gpa: number): string {
  throw new Error("Not implemented");
}
