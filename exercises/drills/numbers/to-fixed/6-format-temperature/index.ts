/**
 * Format a temperature to 1 decimal place with a degree unit.
 *
 * Use: .toFixed, template literal
 *
 * @example formatTemp(98.678, "F") → "98.7°F"
 * @example formatTemp(36.6, "C") → "36.6°C"
 */
export function formatTemp(temp: number, unit: string): string {
  throw new Error("Not implemented");
}
