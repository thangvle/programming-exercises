/**
 * Format a number to 2 decimal places with a "$" prefix.
 *
 * Use: .toFixed
 *
 * @example formatCurrency(5.1) → "$5.10"
 * @example formatCurrency(100) → "$100.00"
 */
export function formatCurrency(amount: number): string {
  throw new Error("Not implemented");
}
