/**
 * Format a ratio to 3 decimal places.
 *
 * Use: division, .toFixed
 *
 * @example formatRatio(1, 3) → "0.333"
 * @example formatRatio(1, 2) → "0.500"
 */
export function formatRatio(numerator: number, denominator: number): string {
  throw new Error("Not implemented");
}
