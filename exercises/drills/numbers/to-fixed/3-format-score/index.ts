/**
 * Format a score to 1 decimal place.
 *
 * Use: .toFixed
 *
 * @example formatScore(9.5678) → "9.6"
 * @example formatScore(10) → "10.0"
 */
export function formatScore(score: number): string {
  throw new Error("Not implemented");
}
