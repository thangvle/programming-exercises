/**
 * Snap a number to the nearest grid point.
 *
 * Use: Math.round, division, multiplication
 *
 * @example snapToGrid(7, 5) → 5
 * @example snapToGrid(8, 5) → 10
 */
export function snapToGrid(n: number, gridSize: number): number {
  throw new Error("Not implemented");
}
