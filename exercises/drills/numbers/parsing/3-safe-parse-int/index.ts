/**
 * Parse a string to an integer, returning 0 if the result is NaN.
 *
 * Use: parseInt, isNaN or Number.isNaN
 *
 * @example safeParseInt("42") → 42
 * @example safeParseInt("abc") → 0
 */
export function safeParseInt(str: string): number {
  throw new Error("Not implemented");
}
