/**
 * Sum an array of numeric strings.
 *
 * Use: .map, .reduce, Number or parseFloat
 *
 * @example sumStrings(["1", "2", "3"]) → 6
 * @example sumStrings(["10", "20"]) → 30
 */
export function sumStrings(strings: string[]): number {
  throw new Error("Not implemented");
}
