/**
 * Extract all numbers from a string.
 *
 * Use: .match with regex, .map, parseInt/Number
 *
 * @example extractNumbers("I have 3 cats and 2 dogs") → [3, 2]
 * @example extractNumbers("no numbers here") → []
 */
export function extractNumbers(str: string): number[] {
  throw new Error("Not implemented");
}
