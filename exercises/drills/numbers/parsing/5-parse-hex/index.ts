/**
 * Parse a hexadecimal string to a number.
 *
 * Use: parseInt with radix 16
 *
 * @example parseHex("ff") → 255
 * @example parseHex("1a") → 26
 */
export function parseHex(hex: string): number {
  throw new Error("Not implemented");
}
