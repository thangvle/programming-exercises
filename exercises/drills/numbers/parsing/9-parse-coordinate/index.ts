/**
 * Parse a "x,y" string into an {x, y} object with numeric values.
 *
 * Use: .split, Number or parseFloat
 *
 * @example parseCoordinate("10,20") → {x: 10, y: 20}
 * @example parseCoordinate("0,0") → {x: 0, y: 0}
 */
export function parseCoordinate(str: string): { x: number; y: number } {
  throw new Error("Not implemented");
}
