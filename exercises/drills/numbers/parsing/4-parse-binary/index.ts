/**
 * Parse a binary string to a number.
 *
 * Use: parseInt with radix 2
 *
 * @example parseBinary("1010") → 10
 * @example parseBinary("1111") → 15
 */
export function parseBinary(binary: string): number {
  throw new Error("Not implemented");
}
