/**
 * Mask all but the last 4 characters of a string with "*".
 * If the string is 4 characters or shorter, return it unchanged.
 *
 * Use: str.slice(), str.repeat()
 *
 * Examples:
 *   maskCreditCard("1234567812345678")  → "************5678"
 *   maskCreditCard("4111111111111111")  → "************1111"
 *   maskCreditCard("12345")             → "*2345"
 *   maskCreditCard("1234")              → "1234"
 */
export function maskCreditCard(cardNumber: string): string {
  throw new Error("Not implemented");
}
