/**
 * Return the first `n` characters of a string.
 * If `n` is greater than the string length, return the entire string.
 * If `n` is 0, return an empty string.
 *
 * Use: str.slice()
 *
 * Examples:
 *   getFirstNChars("hello", 3)  → "hel"
 *   getFirstNChars("world", 1)  → "w"
 *   getFirstNChars("hi", 10)   → "hi"
 *   getFirstNChars("test", 0)  → ""
 */
export function getFirstNChars(str: string, n: number): string {
  throw new Error("Not implemented");
}
