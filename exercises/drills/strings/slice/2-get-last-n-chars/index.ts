/**
 * Return the last `n` characters of a string.
 * If `n` is greater than the string length, return the entire string.
 * If `n` is 0, return an empty string.
 *
 * Use: str.slice()
 *
 * Examples:
 *   getLastNChars("hello", 3)  → "llo"
 *   getLastNChars("world", 1)  → "d"
 *   getLastNChars("hi", 10)   → "hi"
 *   getLastNChars("test", 0)  → ""
 */
export function getLastNChars(str: string, n: number): string {
  throw new Error("Not implemented");
}
