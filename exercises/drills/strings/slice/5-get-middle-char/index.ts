/**
 * Return the middle character(s) of a string.
 * - If the string has an odd length, return the single middle character.
 * - If the string has an even length, return the two middle characters.
 *
 * Use: str.slice(), Math.floor()
 *
 * Examples:
 *   getMiddleChar("hello")  → "l"      (length 5, index 2)
 *   getMiddleChar("abcd")   → "bc"     (length 4, indices 1-2)
 *   getMiddleChar("a")      → "a"
 *   getMiddleChar("ab")     → "ab"
 *   getMiddleChar("cat")    → "a"
 */
export function getMiddleChar(str: string): string {
  throw new Error("Not implemented");
}
