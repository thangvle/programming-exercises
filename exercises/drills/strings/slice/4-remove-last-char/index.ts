/**
 * Remove the last character from a string and return the rest.
 *
 * Use: str.slice()
 *
 * Examples:
 *   removeLastChar("hello")  → "hell"
 *   removeLastChar("a")      → ""
 *   removeLastChar("hi ")    → "hi"
 *   removeLastChar("123")    → "12"
 */
export function removeLastChar(str: string): string {
  throw new Error("Not implemented");
}
