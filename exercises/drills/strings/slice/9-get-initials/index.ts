/**
 * Given a full name, return the initials (the first character of each word).
 *
 * Use: str.split(), str.slice()
 *
 * Examples:
 *   getInitials("John Doe")            → "JD"
 *   getInitials("Mary Jane Watson")    → "MJW"
 *   getInitials("Cher")                → "C"
 *   getInitials("bob smith")           → "bs"
 *   getInitials("Juan Carlos De Leon") → "JCDL"
 */
export function getInitials(name: string): string {
  throw new Error("Not implemented");
}
