/**
 * Extract a portion of a string from index `start` up to (but not including) index `end`.
 *
 * Use: str.slice()
 *
 * Examples:
 *   extractSubstring("hello world", 0, 5)   → "hello"
 *   extractSubstring("hello world", 6, 11)  → "world"
 *   extractSubstring("abcdef", 2, 3)        → "c"
 *   extractSubstring("abc", 0, 3)           → "abc"
 *   extractSubstring("hello", 2, 2)         → ""
 */
export function extractSubstring(str: string, start: number, end: number): string {
  throw new Error("Not implemented");
}
