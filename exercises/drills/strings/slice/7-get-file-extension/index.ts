/**
 * Given a filename, return the file extension (everything after the last dot).
 *
 * Use: str.lastIndexOf(), str.slice()
 *
 * Examples:
 *   getFileExtension("photo.png")       → "png"
 *   getFileExtension("index.ts")        → "ts"
 *   getFileExtension("archive.tar.gz")  → "gz"
 *   getFileExtension("readme.txt")      → "txt"
 *   getFileExtension("page.html")       → "html"
 */
export function getFileExtension(filename: string): string {
  throw new Error("Not implemented");
}
