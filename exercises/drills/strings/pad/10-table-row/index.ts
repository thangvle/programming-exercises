/**
 * Format a name and score as a table row.
 * Name is left-aligned padded to 15 characters, score is left-aligned padded to 5 characters.
 *
 * Use: str.padEnd()
 *
 * @example
 * tableRow("Alice", 95) // → "Alice          95   "
 * tableRow("Bob", 100) // → "Bob            100  "
 */
export function tableRow(name: string, score: number): string {
  throw new Error("Not implemented");
}
