/**
 * Pad a hex value to 2 characters with leading zeros.
 *
 * Use: str.padStart()
 *
 * @example
 * padHex("f") // → "0f"
 * padHex("ff") // → "ff"
 */
export function padHex(hex: string): string {
  throw new Error("Not implemented");
}
