/**
 * Center a string within a given width using spaces.
 * If the padding is odd, add the extra space to the right.
 *
 * Use: str.padStart(), str.padEnd()
 *
 * @example
 * centerText("hi", 10) // → "    hi    "
 * centerText("hey", 10) // → "   hey    "
 */
export function centerText(str: string, width: number): string {
  throw new Error("Not implemented");
}
