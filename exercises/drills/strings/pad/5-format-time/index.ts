/**
 * Format hours and minutes with leading zeros as "HH:MM".
 *
 * Use: str.padStart()
 *
 * @example
 * formatTime(9, 5) // → "09:05"
 * formatTime(12, 30) // → "12:30"
 */
export function formatTime(hours: number, minutes: number): string {
  throw new Error("Not implemented");
}
