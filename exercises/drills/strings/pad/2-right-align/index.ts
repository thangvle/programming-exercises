/**
 * Right-align a string to a given width by padding with leading spaces.
 *
 * Use: str.padStart()
 *
 * @example
 * rightAlign("hi", 10) // → "        hi"
 * rightAlign("hello", 10) // → "     hello"
 */
export function rightAlign(str: string, width: number): string {
  throw new Error("Not implemented");
}
