/**
 * Format a price as "$X.XX" given dollars and cents.
 * Pad the cents to always be 2 digits.
 *
 * Use: str.padStart()
 *
 * @example
 * formatPrice(5, 5) // → "$5.05"
 * formatPrice(12, 99) // → "$12.99"
 */
export function formatPrice(dollars: number, cents: number): string {
  throw new Error("Not implemented");
}
