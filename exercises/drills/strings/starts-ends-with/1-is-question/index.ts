/**
 * Check if a string ends with "?".
 *
 * Use: str.endsWith()
 *
 * @example
 * isQuestion("how are you?") // → true
 * isQuestion("hello") // → false
 */
export function isQuestion(str: string): boolean {
  throw new Error("Not implemented");
}
