/**
 * Check if a filename ends with a given extension.
 *
 * Use: str.endsWith()
 *
 * @example
 * hasFileType("photo.png", "png") // → true
 * hasFileType("photo.png", "jpg") // → false
 */
export function hasFileType(filename: string, extension: string): boolean {
  throw new Error("Not implemented");
}
