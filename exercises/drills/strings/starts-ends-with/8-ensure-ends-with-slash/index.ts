/**
 * Add "/" at the end of a string if it doesn't already end with one.
 *
 * Use: str.endsWith()
 *
 * @example
 * ensureSlash("/path") // → "/path/"
 * ensureSlash("/path/") // → "/path/"
 */
export function ensureSlash(path: string): string {
  throw new Error("Not implemented");
}
