/**
 * Check if a string starts with a vowel (a, e, i, o, u), case-insensitive.
 *
 * Use: str.startsWith(), str.toLowerCase()
 *
 * @example
 * startsWithVowel("apple") // → true
 * startsWithVowel("banana") // → false
 */
export function startsWithVowel(str: string): boolean {
  throw new Error("Not implemented");
}
