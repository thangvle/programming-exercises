/**
 * Replace all tab characters with 2 spaces.
 *
 * Use: str.replaceAll()
 *
 * @example tabsToSpaces("a\tb") → "a  b"
 * @example tabsToSpaces("\thello") → "  hello"
 */
export function tabsToSpaces(str: string): string {
  throw new Error("Not implemented");
}
