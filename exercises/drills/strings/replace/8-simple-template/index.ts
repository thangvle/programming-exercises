/**
 * Replace {{name}} with a given value in a template string.
 *
 * Use: str.replaceAll()
 *
 * @example fillTemplate("Hello {{name}}", "Alice") → "Hello Alice"
 * @example fillTemplate("{{name}} is {{name}}", "Bob") → "Bob is Bob"
 */
export function fillTemplate(template: string, value: string): string {
  throw new Error("Not implemented");
}
