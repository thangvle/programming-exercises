/**
 * Replace all digits with "#".
 *
 * Use: str.replace() with regex or str.replaceAll()
 *
 * @example redactNumbers("call 555-1234") → "call ###-####"
 * @example redactNumbers("age: 25") → "age: ##"
 */
export function redactNumbers(str: string): string {
  throw new Error("Not implemented");
}
