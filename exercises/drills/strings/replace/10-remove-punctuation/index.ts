/**
 * Remove . , ! ? from a string.
 *
 * Use: str.replaceAll()
 *
 * @example removePunctuation("Hello, world!") → "Hello world"
 * @example removePunctuation("Wait... what?") → "Wait what"
 */
export function removePunctuation(str: string): string {
  throw new Error("Not implemented");
}
