/**
 * Replace a specific word with "****".
 *
 * Use: str.replace()
 *
 * @example censorWord("oh dang it", "dang") → "oh **** it"
 * @example censorWord("hello world", "world") → "hello ****"
 */
export function censorWord(str: string, word: string): string {
  throw new Error("Not implemented");
}
