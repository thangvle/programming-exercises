/**
 * Remove all vowels (a, e, i, o, u) from a string.
 *
 * Use: str.replaceAll()
 *
 * @example removeVowels("hello") → "hll"
 * @example removeVowels("banana") → "bnn"
 */
export function removeVowels(str: string): string {
  throw new Error("Not implemented");
}
