/**
 * Replace double spaces with single spaces.
 *
 * Use: str.replaceAll()
 *
 * @example fixDoubleSpaces("hello  world") → "hello world"
 * @example fixDoubleSpaces("a  b  c") → "a b c"
 */
export function fixDoubleSpaces(str: string): string {
  throw new Error("Not implemented");
}
