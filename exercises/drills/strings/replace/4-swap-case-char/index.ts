/**
 * Replace all occurrences of a character with another character.
 *
 * Use: str.replaceAll()
 *
 * @example swapChar("hello", "l", "r") → "herro"
 * @example swapChar("banana", "a", "o") → "bonono"
 */
export function swapChar(str: string, from: string, to: string): string {
  throw new Error("Not implemented");
}
