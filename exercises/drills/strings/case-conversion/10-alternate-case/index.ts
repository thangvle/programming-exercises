/**
 * Alternate between uppercase and lowercase for each character.
 * Start with lowercase at index 0.
 *
 * Use: str.toUpperCase(), str.toLowerCase()
 *
 * @example
 * alternateCase("hello") // → "hElLo"
 * alternateCase("world") // → "wOrLd"
 */
export function alternateCase(str: string): string {
  throw new Error("Not implemented");
}
