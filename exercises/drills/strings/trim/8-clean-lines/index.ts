/**
 * Split by newline, trim each line, and filter out empty lines.
 *
 * Use: str.split(), str.trim(), array.filter()
 *
 * @example cleanLines("  hello\n  \n  world  ") → ["hello", "world"]
 * @example cleanLines("a\nb\nc") → ["a", "b", "c"]
 */
export function cleanLines(str: string): string[] {
  throw new Error("Not implemented");
}
