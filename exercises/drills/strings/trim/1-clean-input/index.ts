/**
 * Trim whitespace from both ends of a string.
 *
 * Use: str.trim()
 *
 * @example cleanInput("  hello  ") → "hello"
 * @example cleanInput("\thello\n") → "hello"
 */
export function cleanInput(str: string): string {
  throw new Error("Not implemented");
}
