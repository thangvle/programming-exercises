/**
 * Trim only trailing whitespace from a string.
 *
 * Use: str.trimEnd()
 *
 * @example trimEndOnly("  hello  ") → "  hello"
 * @example trimEndOnly("hello\n") → "hello"
 */
export function trimEndOnly(str: string): string {
  throw new Error("Not implemented");
}
