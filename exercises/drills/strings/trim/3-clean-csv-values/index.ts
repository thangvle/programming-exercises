/**
 * Split a string by comma and trim each value.
 *
 * Use: str.split(), str.trim()
 *
 * @example cleanCsvValues(" a , b , c ") → ["a", "b", "c"]
 * @example cleanCsvValues("x,y,z") → ["x", "y", "z"]
 */
export function cleanCsvValues(str: string): string[] {
  throw new Error("Not implemented");
}
