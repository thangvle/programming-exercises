/**
 * Remove a specific character from both ends of a string (not just whitespace).
 *
 * Use: loop with indexOf or similar trimming logic
 *
 * @example trimChar("---hello---", "-") → "hello"
 * @example trimChar("***star***", "*") → "star"
 */
export function trimChar(str: string, char: string): string {
  throw new Error("Not implemented");
}
