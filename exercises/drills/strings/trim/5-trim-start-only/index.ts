/**
 * Trim only leading whitespace from a string.
 *
 * Use: str.trimStart()
 *
 * @example trimStartOnly("  hello  ") → "hello  "
 * @example trimStartOnly("\thello") → "hello"
 */
export function trimStartOnly(str: string): string {
  throw new Error("Not implemented");
}
