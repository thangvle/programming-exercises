/**
 * Find the first character that appears in both strings.
 * Return an empty string if no common character is found.
 *
 * Use: str.includes() or str.indexOf()
 *
 * @example findCommonChar("abc", "cde") → "c"
 * @example findCommonChar("abc", "xyz") → ""
 */
export function findCommonChar(str1: string, str2: string): string {
  throw new Error("Not implemented");
}
