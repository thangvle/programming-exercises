/**
 * Check if a string has both "(" and ")".
 *
 * Use: str.includes()
 *
 * @example hasMatchingBrackets("(hello)") → true
 * @example hasMatchingBrackets("hello)") → false
 */
export function hasMatchingBrackets(str: string): boolean {
  throw new Error("Not implemented");
}
