/**
 * Find the index of the second occurrence of a character in a string.
 * Return -1 if the character appears fewer than two times.
 *
 * Use: str.indexOf() with fromIndex parameter
 *
 * @example findSecondOccurrence("hello", "l") → 3
 * @example findSecondOccurrence("hello", "h") → -1
 */
export function findSecondOccurrence(str: string, char: string): number {
  throw new Error("Not implemented");
}
