/**
 * Find the index of a substring within a string.
 *
 * Use: str.indexOf()
 *
 * @example findPosition("hello world", "world") → 6
 * @example findPosition("hello world", "xyz") → -1
 */
export function findPosition(str: string, substring: string): number {
  throw new Error("Not implemented");
}
