/**
 * Check if a sentence contains a specific word.
 *
 * Use: str.includes()
 *
 * @example containsWord("hello world", "world") → true
 * @example containsWord("hello world", "planet") → false
 */
export function containsWord(sentence: string, word: string): boolean {
  throw new Error("Not implemented");
}
