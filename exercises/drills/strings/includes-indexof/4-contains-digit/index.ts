/**
 * Check if a string contains any digit (0-9).
 *
 * Use: str.includes()
 *
 * @example containsDigit("abc3def") → true
 * @example containsDigit("abcdef") → false
 */
export function containsDigit(str: string): boolean {
  throw new Error("Not implemented");
}
