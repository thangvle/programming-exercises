/**
 * Check if a string contains any of the given substrings.
 *
 * Use: str.includes() with array iteration
 *
 * @example containsAny("hello world", ["xyz", "world"]) → true
 * @example containsAny("hello world", ["xyz", "abc"]) → false
 */
export function containsAny(str: string, substrings: string[]): boolean {
  throw new Error("Not implemented");
}
