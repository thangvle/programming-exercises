/**
 * Return the last word of a sentence.
 * Words are separated by spaces.
 *
 * Use: str.split()
 *
 * Examples:
 *   getLastWord("hello world")          → "world"
 *   getLastWord("the quick brown fox")  → "fox"
 *   getLastWord("hello")                → "hello"
 *   getLastWord("the answer is 42")     → "42"
 */
export function getLastWord(sentence: string): string {
  throw new Error("Not implemented");
}
