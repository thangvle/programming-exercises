/**
 * Split a sentence into words and return an array where each element
 * is the number of vowels (a, e, i, o, u) in the corresponding word.
 * Vowel matching should be case-insensitive.
 *
 * Use: str.split(), str.match() or loop through characters
 *
 * Examples:
 *   countVowelsPerWord("hello world")  → [2, 1]
 *   countVowelsPerWord("apple")        → [2]
 *   countVowelsPerWord("gym cry")      → [0, 0]
 *   countVowelsPerWord("aeiou")        → [5]
 *   countVowelsPerWord("Hello World")  → [2, 1]
 */
export function countVowelsPerWord(sentence: string): number[] {
  throw new Error("Not implemented");
}
