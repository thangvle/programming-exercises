/**
 * Capitalize the first letter of each word in a sentence.
 * The rest of each word should remain unchanged.
 *
 * Use: str.split(), str.slice(), str.toUpperCase(), arr.join()
 *
 * Examples:
 *   capitalizeWords("hello world")            → "Hello World"
 *   capitalizeWords("hello")                  → "Hello"
 *   capitalizeWords("the quick brown fox")    → "The Quick Brown Fox"
 *   capitalizeWords("a b c")                  → "A B C"
 *   capitalizeWords("Hello World")            → "Hello World"
 */
export function capitalizeWords(sentence: string): string {
  throw new Error("Not implemented");
}
