/**
 * Split a comma-separated string into an array of trimmed values.
 * Each value should have leading and trailing whitespace removed.
 *
 * Use: str.split(), str.trim()
 *
 * Examples:
 *   csvToArray("a,b,c")              → ["a", "b", "c"]
 *   csvToArray("a, b, c")            → ["a", "b", "c"]
 *   csvToArray("hello")              → ["hello"]
 *   csvToArray("red , green , blue") → ["red", "green", "blue"]
 */
export function csvToArray(csv: string): string[] {
  throw new Error("Not implemented");
}
