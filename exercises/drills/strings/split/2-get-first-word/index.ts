/**
 * Return the first word of a sentence.
 * Words are separated by spaces.
 *
 * Use: str.split()
 *
 * Examples:
 *   getFirstWord("hello world")          → "hello"
 *   getFirstWord("the quick brown fox")  → "the"
 *   getFirstWord("hello")                → "hello"
 *   getFirstWord("42 is the answer")     → "42"
 */
export function getFirstWord(sentence: string): string {
  throw new Error("Not implemented");
}
