/**
 * Given an email address, return the domain part (everything after the "@").
 *
 * Use: str.split()
 *
 * Examples:
 *   getDomain("user@example.com")        → "example.com"
 *   getDomain("john@gmail.com")          → "gmail.com"
 *   getDomain("admin@mail.company.org")  → "mail.company.org"
 *   getDomain("user+tag@example.com")    → "example.com"
 */
export function getDomain(email: string): string {
  throw new Error("Not implemented");
}
