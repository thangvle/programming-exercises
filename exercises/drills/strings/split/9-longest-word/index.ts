/**
 * Return the longest word in a sentence.
 * If multiple words share the longest length, return the first one.
 *
 * Use: str.split(), loop or arr.reduce()
 *
 * Examples:
 *   longestWord("I love programming")  → "programming"
 *   longestWord("elephant is big")     → "elephant"
 *   longestWord("hello")               → "hello"
 *   longestWord("cat dog rat")         → "cat"  (tie → first wins)
 *   longestWord("a bb ccc")            → "ccc"
 */
export function longestWord(sentence: string): string {
  throw new Error("Not implemented");
}
