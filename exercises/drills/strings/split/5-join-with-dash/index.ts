/**
 * Split a sentence into words, then join them back together with dashes.
 *
 * Use: str.split(), arr.join()
 *
 * Examples:
 *   joinWithDash("hello world")    → "hello-world"
 *   joinWithDash("one two three")  → "one-two-three"
 *   joinWithDash("hello")          → "hello"
 *   joinWithDash("my blog post")   → "my-blog-post"
 */
export function joinWithDash(sentence: string): string {
  throw new Error("Not implemented");
}
