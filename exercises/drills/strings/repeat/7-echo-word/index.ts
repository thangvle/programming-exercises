/**
 * Repeat a word n times separated by spaces.
 *
 * Use: str.repeat()
 *
 * @example
 * echoWord("hey", 3) // → "hey hey hey"
 * echoWord("ok", 2) // → "ok ok"
 */
export function echoWord(word: string, n: number): string {
  throw new Error("Not implemented");
}
