/**
 * Surround a string with a character repeated n times on both sides.
 *
 * Use: str.repeat()
 *
 * @example
 * surround("hello", "*", 3) // → "***hello***"
 * surround("hi", "-", 2) // → "--hi--"
 */
export function surround(str: string, char: string, n: number): string {
  throw new Error("Not implemented");
}
