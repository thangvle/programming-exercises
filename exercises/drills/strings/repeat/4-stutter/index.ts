/**
 * Repeat the first 2 characters of a word 3 times.
 *
 * Use: str.repeat()
 *
 * @example
 * stutter("hello") // → "hehehe"
 * stutter("world") // → "wowowo"
 */
export function stutter(word: string): string {
  throw new Error("Not implemented");
}
