/**
 * Create a progress bar given current and total values.
 * The bar is 10 characters wide, using "#" for filled and "-" for empty,
 * wrapped in square brackets.
 *
 * Use: str.repeat()
 *
 * @example
 * progressBar(3, 10) // → "[###-------]"
 * progressBar(10, 10) // → "[##########]"
 */
export function progressBar(current: number, total: number): string {
  throw new Error("Not implemented");
}
