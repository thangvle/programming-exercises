/**
 * Create a row alternating two characters for a given length.
 *
 * Use: str.repeat()
 *
 * @example
 * checkerboardRow("X", "O", 6) // → "XOXOXO"
 * checkerboardRow("A", "B", 5) // → "ABABA"
 */
export function checkerboardRow(a: string, b: string, length: number): string {
  throw new Error("Not implemented");
}
