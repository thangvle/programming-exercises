/**
 * Create a single-line bordered box around text.
 * Uses "+", "-", and "|" characters with a space padding around the text.
 *
 * Use: str.repeat()
 *
 * @example
 * borderBox("hi") // → "+----+\n| hi |\n+----+"
 * borderBox("hello") // → "+-------+\n| hello |\n+-------+"
 */
export function borderBox(text: string): string {
  throw new Error("Not implemented");
}
