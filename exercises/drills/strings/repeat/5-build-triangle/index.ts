/**
 * Build a right triangle of stars, each line on a new line.
 *
 * Use: str.repeat()
 *
 * @example
 * buildTriangle(3) // → "*\n**\n***"
 * buildTriangle(1) // → "*"
 */
export function buildTriangle(height: number): string {
  throw new Error("Not implemented");
}
