/**
 * Indent a string with n spaces.
 *
 * Use: str.repeat()
 *
 * @example
 * indent("hello", 4) // → "    hello"
 * indent("world", 2) // → "  world"
 */
export function indent(str: string, n: number): string {
  throw new Error("Not implemented");
}
